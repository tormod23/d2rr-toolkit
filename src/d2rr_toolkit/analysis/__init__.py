"""Diagnostic / reverse-engineering helpers.

This package hosts read-only analyzers used to study d2i / d2s binary
structure. Nothing here is on the parse / write hot path - the modules
exist so investigations into still-undocumented format regions can be
reproduced and shared as scripts rather than ad-hoc one-offs.
"""
